#include "HashTable.h" 

namespace HashTable
{

	// implementation of a basic addition hash     
	unsigned int badHash(const char* data, unsigned int length)
	{
		unsigned int hash = 0;
		for (unsigned int i = 0; i < length; ++i) 
		{
			hash += data[i];
		}




		return hash;
	}

	bool isEmpty(unsigned int length, )
	{
		int total{};
		for (int i{}; i < length; i ++)
		{
			total += HashTable

		}


		return false;
	}

	void insert(unsigned int key, const char * data)
	{
	}

	void remove(unsigned int key)
	{
	}

	const char * searchTable(unsigned int key)
	{
		return nullptr;
	}

	void print()
	{
	}

}
